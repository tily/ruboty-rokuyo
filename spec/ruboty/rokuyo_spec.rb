require 'spec_helper'

describe Ruboty::Rokuyo do
  it 'has a version number' do
    expect(Ruboty::Rokuyo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
